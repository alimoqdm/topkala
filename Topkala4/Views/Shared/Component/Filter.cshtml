@model IEnumerable<DataLayer.Category>


@if (Model.Count() != 0)
{
    @foreach (var item in Model)
    {

        <div class="row">
            <input type="checkbox" id="myCheck" value="@item.CategoryId" onclick="myFunction()" />
            <label class="">@item.Name</label>
        </div>

        <p id="text" style="display:none">Checkbox is CHECKED!</p>
    }

}
else
{
    <div class="row">
        <input id="chekbox" type="radio" />

        <label>@ViewBag.Filter</label>

    </div>
}



<style>

    input[type="checkbox"]:checked {
        width: 20px;
        height: 20px;
    }

    input[type="checkbox"] {
        width: 20px;
        height: 20px;
        margin-left: 8px;
    }
</style>

@section Scripts {

    <script>

        function myFunction() {
  // Get the checkbox
  var checkBox = document.getElementById("myCheck");
  // Get the output text
  var text = document.getElementById("text");

  // If the checkbox is checked, display the output text
  if (checkBox.checked == true){
    text.style.display = "block";
  } else {
    text.style.display = "none";
  }
}
        //function myFunction(uniqueId) {

        //    var Chek = 'myCheck_' + uniqueId;
        //    var checkBox = document.getElementById(Chek);
        //    var id = $("#" + Chek).val();

        //    if ($("#" + checkBox).prop("checked", true)) {

        //        $.ajax({
        //            url: '/Search/CategoryProduct/' + id,
        //            type: 'Get',

        //        });

        //    }


        //}

    </script>
}
